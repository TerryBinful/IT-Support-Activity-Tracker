<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:9px}h1{font-size:18px;margin-bottom:4px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #ddd;padding:4px;text-align:left;vertical-align:top}th{background:#f3f4f6}</style></head><body><h1>IT Activity Report</h1><p>{{ $from }} to {{ $to }}</p><table><thead><tr>@foreach($order as $key)<th>{{ $labels[$key] ?? $key }}</th>@endforeach</tr></thead><tbody>@foreach($activities as $a)<tr>@foreach($order as $key)<td>{{ match($key){'activity_date'=>$a->activity_date?->format('Y-m-d'),'created_at'=>$a->created_at?->format('Y-m-d H:i'),'category'=>$a->category?->name,'started_at'=>$a->started_at?->format('Y-m-d H:i'),'completed_at'=>$a->completed_at?->format('Y-m-d H:i'),'follow_up_required'=>$a->follow_up_required?'Yes':'No',default=>$a->{$key}} }}</td>@endforeach</tr>@endforeach</tbody></table></body></html>
