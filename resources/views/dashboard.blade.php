@extends('layouts.app')
@section('content')<div class="flex items-end justify-between"><div><p class="text-sm text-slate-500">{{ now()->format('l, d F Y') }}</p><h1 class="text-3xl font-bold">Dashboard</h1></div><a class="btn" href="{{ route('activities.create') }}">+ Add Activity</a></div>@if($reminder)<div class="mt-6 rounded-xl border border-amber-200 bg-amber-50 p-5"><div class="font-semibold">Monthly report reminder</div><p class="mt-1 text-sm text-amber-900">It's the last Friday. Your monthly activity report is ready to review.</p><a href="{{ route('reports.index') }}" class="mt-3 inline-block font-semibold underline">Review report →</a></div>@endif<div class="mt-7 grid gap-4 md:grid-cols-4">@foreach([['Today',$stats['today']],['This month',$stats['month']],['Completed',$stats['completed']],['Open',$stats['open']]] as [$label,$value])<div class="rounded-xl border bg-white p-5"><div class="text-sm text-slate-500">{{ $label }}</div><div class="mt-2 text-3xl font-bold">{{ $value }}</div></div>@endforeach</div><div class="mt-8 overflow-hidden rounded-xl border bg-white"><div class="flex items-center justify-between border-b p-5"><h2 class="font-semibold">Today's activities</h2><a href="{{ route('activities.index') }}" class="text-sm font-semibold">View all</a></div><div class="divide-y">@forelse($today as $a)<div class="flex items-center justify-between p-5"><div><div class="font-medium">{{ $a->title }}</div><div class="text-sm text-slate-500">{{ $a->category?->name ?? 'Uncategorised' }} · {{ str($a->status)->replace('_',' ')->title() }}</div></div><div class="text-sm text-slate-500">{{ $a->created_at?->format('H:i') }}</div></div>@empty<div class="p-8 text-center text-slate-500">No activities logged today.</div>@endforelse</div></div>@endsection
